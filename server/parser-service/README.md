# 🚀 Document Parser Service - Enterprise Edition

Мощный сервис для парсинга документов с продвинутым анализом, поддержкой множества форматов и enterprise-функциями.

## ✨ Основные возможности

### 📄 Поддерживаемые форматы
- **Документы**: PDF, DOCX, XLSX, TXT, HTML, RTF, ODT
- **Данные**: CSV (авто-определение разделителя)
- **Email**: EML с вложениями
- **Изображения**: PNG, JPG, BMP, TIFF (с OCR)
- **Архивы**: ZIP, 7Z, RAR (рекурсивная обработка)

### 🧠 Интеллектуальный анализ

#### Named Entity Recognition (NER)
- Email адреса
- Телефоны (российские форматы)
- URL адреса
- Даты (множество форматов)
- Денежные суммы (руб, USD, EUR)
- ИНН, КПП, ОГРН
- ФИО (русские имена)

#### Классификация документов
- Счета и инвойсы
- Договоры и контракты
- Резюме и CV
- Отчеты
- Приказы
- Выписки
- Спецификации
- Акты
- Письма
- Презентации

#### Семантический анализ
- Извлечение ключевых слов (TF-IDF)
- Тематическое моделирование (LDA)
- Статистика текста
- Автоматическая саммаризация
- Определение языка (70+ языков)

### 🔒 Enterprise безопасность
- JWT аутентификация
- Role-Based Access Control (RBAC)
- Audit logging всех операций
- Валидация и санитизация входных данных
- Защита от вредоносных файлов

### ⚡ Масштабируемость
- Пакетная обработка документов
- Приоритеты задач
- Асинхронная обработка
- Мониторинг производительности
- Redis кеширование (опционально)

### 🧹 Обработка данных
- Автоматическая очистка текста
- Исправление кодировки
- Объединение разорванных строк
- Удаление колонтитулов
- Нормализация значений

## 🚀 Быстрый старт

### Установка зависимостей

```bash
cd /root/secure-messenger/server/parser-service
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

### Дополнительные зависимости

```bash
# Tesseract для OCR
sudo apt-get install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng

# python-magic
sudo apt-get install libmagic1
```

### Запуск сервиса

```bash
python main.py
```

Сервис будет доступен на `http://localhost:8000`

## 📖 API Документация

### Базовый парсинг

**POST /parse**

Парсинг документа с опциональным анализом.

```bash
curl -X POST "http://localhost:8000/parse" \
  -F "file=@document.pdf" \
  -F "enable_ner=true" \
  -F "enable_classification=true" \
  -F "enable_semantic_analysis=false" \
  -F "enable_language_detection=true" \
  -F "clean_text=false"
```

**Параметры:**
- `file`: Файл для парсинга (обязательный)
- `enable_ner`: Включить NER (по умолчанию: true)
- `enable_classification`: Включить классификацию (по умолчанию: true)
- `enable_semantic_analysis`: Включить семантический анализ (по умолчанию: false)
- `enable_language_detection`: Включить определение языка (по умолчанию: true)
- `clean_text`: Включить очистку текста (по умолчанию: false)

**Ответ:**

```json
{
  "metadata": {
    "filename": "document.pdf",
    "type": "pdf",
    "size": 1024000,
    "size_mb": 0.98,
    "pages": 10,
    "word_count": 5420,
    "parsed_at": "2024-01-15T10:30:00.000Z"
  },
  "content": {
    "text": "Полный текст документа...",
    "structure": [
      {
        "type": "heading_1",
        "text": "Заголовок"
      },
      {
        "type": "paragraph",
        "text": "Текст параграфа..."
      }
    ],
    "tables": [
      {
        "headers": ["Колонка 1", "Колонка 2"],
        "rows": [...]
      }
    ]
  },
  "analysis": {
    "language": {
      "language": "ru",
      "language_name": "Russian",
      "confidence": 0.95
    },
    "entities": {
      "emails": ["info@example.com"],
      "phones": ["+7 (999) 123-45-67"],
      "urls": ["https://example.com"],
      "dates": [...],
      "money": [...],
      "inn": ["1234567890"],
      "fio": ["Иванов Иван Иванович"]
    },
    "classification": {
      "document_type": "contract",
      "document_type_name": "Договор/Contract",
      "confidence": 0.85
    },
    "semantic": {
      "keywords": [
        {"word": "договор", "score": 0.95},
        {"word": "оплата", "score": 0.87}
      ],
      "topics": [...],
      "summary": "Краткое резюме документа..."
    }
  }
}
```

### Экспорт

**POST /export**

Экспорт результатов в различные форматы.

```bash
curl -X POST "http://localhost:8000/export" \
  -H "Content-Type: application/json" \
  -d '{
    "data": { "результаты парсинга" },
    "format": "json",
    "options": {}
  }'
```

**Поддерживаемые форматы:**
- `json`: JSON
- `text`: Plain text
- `markdown`: Markdown
- `excel`: Excel (XLSX)
- `html`: HTML

### Информация о форматах

**GET /formats**

Список поддерживаемых форматов.

```bash
curl http://localhost:8000/formats
```

### Проверка здоровья

**GET /health**

Проверка работоспособности сервиса.

```bash
curl http://localhost:8000/health
```

## 🔧 Конфигурация

### Основные настройки

```python
# main.py

# Включить/выключить функции по умолчанию
DEFAULT_ENABLE_NER = True
DEFAULT_ENABLE_CLASSIFICATION = True
DEFAULT_ENABLE_SEMANTIC = False
DEFAULT_ENABLE_LANGUAGE = True

# Безопасность
JWT_SECRET_KEY = "your-secret-key"  # ИЗМЕНИТЬ В ПРОДАКШЕНЕ!
JWT_ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

# Масштабирование
MAX_BATCH_WORKERS = 4
MAX_FILE_SIZE = 100 * 1024 * 1024  # 100 MB
```

### Роли и разрешения

```python
# security/rbac.py

# Роли
GUEST = "guest"           # Ограниченный доступ
USER = "user"             # Обычный пользователь  
POWER_USER = "power_user" # Продвинутый пользователь
ADMIN = "admin"           # Администратор
SUPER_ADMIN = "super_admin"  # Супер-администратор

# Разрешения для каждой роли настраиваются в ROLE_PERMISSIONS
```

## 📊 Примеры использования

### Python

```python
import requests

# Парсинг PDF с полным анализом
with open('document.pdf', 'rb') as f:
    response = requests.post(
        'http://localhost:8000/parse',
        files={'file': f},
        data={
            'enable_ner': 'true',
            'enable_classification': 'true',
            'enable_semantic_analysis': 'true',
            'enable_language_detection': 'true',
        }
    )

result = response.json()
print(f"Язык: {result['analysis']['language']['language_name']}")
print(f"Тип: {result['analysis']['classification']['document_type_name']}")
print(f"Найдено email: {len(result['analysis']['entities']['emails'])}")
```

### JavaScript

```javascript
const formData = new FormData();
formData.append('file', fileInput.files[0]);
formData.append('enable_ner', 'true');
formData.append('enable_classification', 'true');

const response = await fetch('http://localhost:8000/parse', {
    method: 'POST',
    body: formData
});

const result = await response.json();
console.log('Результат:', result);
```

### cURL

```bash
# Парсинг CSV файла
curl -X POST "http://localhost:8000/parse" \
  -F "file=@data.csv" \
  -F "enable_ner=true"

# Парсинг архива
curl -X POST "http://localhost:8000/parse" \
  -F "file=@archive.zip" \
  -F "enable_ner=true" \
  -F "enable_classification=true"

# Парсинг изображения с OCR
curl -X POST "http://localhost:8000/parse" \
  -F "file=@scan.jpg" \
  -F "enable_ner=true"
```

## 🧪 Тестирование

```bash
# Запуск тестов
pytest tests/ -v

# С покрытием
pytest tests/ --cov=. --cov-report=html

# Только быстрые тесты
pytest tests/ -m "not slow"
```

## 📈 Производительность

### Типичное время обработки

| Формат | Размер | Базовый парсинг | С полным анализом |
|--------|--------|-----------------|-------------------|
| PDF    | 1 MB   | ~1 сек          | ~3 сек           |
| DOCX   | 500 KB | ~0.5 сек        | ~2 сек           |
| CSV    | 2 MB   | ~0.3 сек        | ~1 сек           |
| Изображение | 3 MB | ~2 сек      | ~4 сек           |
| ZIP (10 файлов) | 5 MB | ~5 сек   | ~15 сек          |

### Оптимизация

- **Отключите семантический анализ** для простых задач (самая ресурсоемкая операция)
- **Используйте пакетную обработку** для множества файлов
- **Включите Redis** для кеширования результатов
- **Увеличьте max_workers** для параллельной обработки

## 🐛 Отладка

### Включение DEBUG логов

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Проверка установленных библиотек

```bash
python -c "import pytesseract; print(pytesseract.get_tesseract_version())"
python -c "import cv2; print(cv2.__version__)"
python -c "import sklearn; print(sklearn.__version__)"
```

## 🤝 Вклад

Для внесения изменений:
1. Создайте ветку для фичи
2. Напишите тесты
3. Убедитесь что все тесты проходят
4. Создайте Pull Request

## 📝 Лицензия

MIT License

## 🎯 Roadmap

### Ближайшие планы
- [ ] Web интерфейс для загрузки файлов
- [ ] PostgreSQL для хранения метаданных
- [ ] S3 интеграция для хранения файлов
- [ ] Webhooks для уведомлений
- [ ] Сравнение версий документов
- [ ] Поиск дубликатов

### Долгосрочные планы
- [ ] Fine-tuned BERT модели для классификации
- [ ] Собственная модель NER для русского языка
- [ ] Поддержка видео и аудио файлов
- [ ] Real-time обработка документов
- [ ] Kubernetes deployment

## 📧 Поддержка

Для вопросов и предложений создавайте Issue на GitHub.

---

**Made with ❤️ by Parser Team**
